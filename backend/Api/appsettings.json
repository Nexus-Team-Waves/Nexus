{
  "//": [
    "PLACEHOLDERS ONLY — this file is committed to source control.",
    "NEVER put a real password, credential, or connection string here (CLAUDE.md §1.2).",
    "Real values belong in appsettings.Development.json / appsettings.Production.json",
    "(git-ignored), user-secrets, or environment variables.",
    "  Local dev:  dotnet user-secrets set \"Sap:ServiceLayer:Password\" \"...\" --project backend/Api"
  ],

  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",

  "Cors": {
    "AllowedOrigins": [ "http://localhost:5173" ]
  },

  "Approval": {
    "//": [
      "Approval-chain tuning. The two Finance emails route claims by the employee's grade:",
      "M-grade employees' claims go to FinanceMGradeEmail, everyone else's to FinanceOtherEmail.",
      "Both addresses are changeable here without a code change."
    ],
    "TopLevelThreshold": 25000,
    "FinanceMGradeEmail": "mapproval@waves.com.pk",
    "FinanceOtherEmail": "otherapproval@waves.com.pk"
  },

  "Push": {
    "//": [
      "Web Push (VAPID) for approver notifications. Keys are SECRETS-adjacent: leave empty here.",
      "When empty, the API generates a key pair on first start and persists it to",
      "App_Data/vapid-keys.json (git-ignored). Set real values via user-secrets/env for production."
    ],
    "Subject": "mailto:mems@waves.com.pk",
    "PublicKey": "",
    "PrivateKey": ""
  },

  "ConnectionStrings": {
    "//": "MEMS DB only. Its own SQL Server 2019 instance (CLAUDE.md §1.5).",
    "MemsDb": "REPLACE_ME"
  },

  "Sap": {
    "//": "SAP B1 10.00 FP2102. Service Layer is primary; DI-API is fallback only (§7).",
    "ServiceLayer": {
      "BaseUrl": "REPLACE_ME",
      "CompanyDb": "REPLACE_ME",
      "Username": "REPLACE_ME",
      "Password": "REPLACE_ME",
      "TimeoutSeconds": 30
    },
    "DiApi": {
      "//": "Fallback transport. Keep disabled until a Service Layer gap forces it.",
      "Enabled": false
    },
    "Posting": {
      "//": [
        "GL accounts and document mapping are UNDECIDED — see /docs/sap-mapping.md.",
        "They live here, in config, so account codes never get scattered through code (§7).",
        "Left empty deliberately: a guessed account code is a financial defect."
      ],
      "MedicalExpenseAccount": "",
      "EmployeePayableAccount": "",
      "DocumentSeries": ""
    }
  }
}
